# ملخص مكتبات Python للذكاء الاصطناعي

## 1. NumPy
- للمصفوفات والأرقام والعمليات الحسابية.
- ابدأ بالمصفوفات، المتوسط، أعلى وأقل قيمة.

## 2. Pandas
- للجداول وملفات CSV.
- تعلم `read_csv()` و`head()` و`info()` و`isnull()` و`mean()`.

## 3. Matplotlib
- لرسم البيانات والنتائج.
- ابدأ بالمخطط الخطي ومخطط الأعمدة.

## 4. scikit-learn
- لنماذج تعلم الآلة مثل التوقع والتصنيف.
- تعلمه بعد فهم البيانات وتنظيفها.

## 5. TensorFlow وPyTorch
- للتعلم العميق والشبكات العصبية.
- اتركهما لمرحلة لاحقة.

## قاعدة مهمة
لا تتعلم المكتبة بشكل نظري فقط. اربط كل مكتبة بمهمة صغيرة أو ملف بيانات أو رسم أو نموذج بسيط.
